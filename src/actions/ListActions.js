import {
    LIST_START,
    LIST_SUCCESS,
    LIST_FAILED,

} from './types'

import { Alert } from 'react-native'


export const getList = (params) => {
    return (dispatch) => {
       
    }
}

export const postPlace = (params) => {
    return (dispatch) => {
        
    }
}


export const removePlace = (params) => {
    return (dispatch) => {
       
    }
}

